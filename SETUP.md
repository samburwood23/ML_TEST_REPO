# 1. Install required packages
pip install flask flask-cors scikit-fuzzy numpy pandas

# 2. Save the Python backend as: women_dei_stress_fuzzy.py

# 3. Run the Flask server
python women_dei_stress_fuzzy.py

# 4. Open the HTML file in your browser
# The frontend will automatically connect to http://localhost:5000